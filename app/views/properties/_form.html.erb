<%= form_for(property) do  |form| %>
  <% if property.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(home.errors.count, "error") %> prohibited this home from being saved:</h2>

      <ul>
      <% home.errors.full_messages.each do |message| %>
        <td><%= message %></td>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :rent%>
    <%= form.number_field :rent %>
  </div>

  <div class="field">
    <%= form.label :address%>
    <%= form.text_field :address %>
  </div>

  <div class="field">
    <%= form.label :year%>
    <%= form.number_field :year %>
  </div>

  <div class="field">
    <%= form.label :remarks %>
    <%= form.text_area :remarks %>
  </div>

<% s = 0 %>
<%= form.fields_for :station do |station| %>
  <h3>Nearest station<%= s += 1 %></h3>
    <tr>
      <td><%= station.label :line %></td>
      <td><%= station.text_field :line %></td>
      <td><%= station.label :name %></td>
      <td><%= station.text_field :name %></td>
      <td><%= station.label :minutes %></td>
      <td><%= station.text_field :minutes %></td>
    </tr>
<% end %>

    <div class="actions">
      <%= form.submit "Save" %>
    </div>
<% end %>
